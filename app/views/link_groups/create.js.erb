(function () {
  let ok = '<%= @ajax_create_group[:ok] %>' === 'true'

  if (ok) {
    $(`#create-group`).modal('hide')
    $('.modal-backdrop').hide()

    $('#create-group .group_name').val('');

    $(`.link_group`).replaceWith("<%= j render('shared/partials/link_ajax') %>")
  } else {
    $(`.error-txt`).removeClass('hidden');
    $('.modal').on('hidden.bs.modal', function (e) {
      $(`.error-txt`).addClass('hidden')
    });
  }
})()
