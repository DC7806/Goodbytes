
<%= form_for @channel , url: organization_channel_index_path(params[:organization_id]) do |f| %>

  <%= f.hidden_field :organization_id, value: params[:organization_id] %>
  <%= f.label :name, "頻道名稱" %>
  <%= f.text_field :name %>
  <%= f.submit "送出", disable_with: '送出ing...' %>
  
<% end %>