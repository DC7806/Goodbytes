(function () {
  let ok = '<%= @ajax_destroy_link[:ok] %>' === 'true'

  if (ok) {
    let group_id = '<%= @saved_link.link_group_id %>'
    let link_id = '<%= @saved_link.id %>'
    $(`#destroy-link-${link_id}`).modal('hide')
    $('.modal-backdrop').hide()


    $(`.link_group`).replaceWith("<%= j render('shared/partials/link_ajax', link_groups: @link_groups, link_group: @saved_link.link_group, new_link_group: @channel.link_groups.new, saved_link: @saved_link, new_saved_link: @link_group.saved_links.new) %>")
  } else {
    alert("can't destroy")
  }
})()